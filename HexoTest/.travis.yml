language: node_js
node_js: stable

before_install:
  - npm install -g hexo-cli
  
  
# S: Build Lifecycle
install:
  - npm install
  - npm install hexo-deployer-git --save


cache:
  directories:
    - node_modules

before_script:
 # - npm install -g gulp
    
script:
  - hexo clean
  - hexo generate

after_script:
  - cd ./public
  - git init
  - git config user.name "gattia"
  - git config user.email "gattia.su@gmail.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${Travis}@${GH_REF}" master:hexo
# E: Build LifeCycle

branches:
  only:
    - master:docs
env:
 global:
   - GH_REF: https://github.com/SylvesterLi/Flora.git

